---
author:
  - 伏枥
theme_name: 色彩斑斓
---
# 1. 风格特点

```CSS
:root {
	--h1-bg: var(--smart-mp-primary);
	--h1-color: white;
	--h1-margin: 1rem;
	--blockquote-background-color: #f0f0f0;
}

/* 引用块样式 */
.smart-mp blockquote {
  background-color: var(--blockquote-background-color, #f0f0f0) !important;
  padding: 1em 1.5em !important;
  border-left: 4px solid rgb(236, 68, 68) !important;
}

```

# 2. Heading
```CSS

h1 .smart-mp-heading-outbox {
	margin: 0 auto;
	padding: 0.5rem 1rem;
	border-radius: 8px;
}

:root {
	--h2-bg: rgb(236, 68, 68);
	--h2-color: white;
	--h2-border-bottom-color: var(--h2-bg);
	--h2-border-bottom-width: 2px;
	--h2-border-bottom-style: solid;
	--h2-outbox-padding:0;
	--h2-leaf-padding: 1rem;
	--h2-outbox-radius: 0.5rem;
	--h2-tail-display: inline-block;
	--h2-tail-shadow-height:2.7rem;
	--h2-tail-shadow-width:1.8rem;
	--h2-tail-shadow-color: rgba(32, 45, 99,0.3);
	--strong-color: rgb(236, 68, 68);
}

:root {

	--h3-border-bottom-color: var(--h3-bg);
	--h3-border-bottom-width: 2px;
	--h3-border-bottom-style: solid;
	--h3-outbox-padding:0;
	
	
}

:root {
	--h4-bg: var(--smart-mp-primary);
	--h4-color: white;
	--h4-border-bottom-color: var(--h4-bg);
	--h4-border-bottom-width: 2px;
	--h4-border-bottom-style: solid;
	--h4-outbox-padding:0;
	--h4-leaf-padding: 1rem;
	--h4-outbox-radius: 0.5rem;
	--h4-tail-display: inline-block;
	--h4-tail-shadow-height:2rem;
	--h4-tail-shadow-width:1.1rem;
	--h4-tail-shadow-color: rgb(239, 235, 233);
	--h4-margin: 1rem 0;
}

/* 段落样式 - 防止数字被误认为标题 */
.smart-mp p {
  border-left: none !important;
  padding-left: 0 !important;
  margin: 1em 0 !important;
}

/* 高亮文本样式 */
.smart-mp mark, .smart-mp .highlight {
  background-color: #ffeb3b !important;
  color: #333 !important;
  padding: 0 2px !important;
  border-radius: 3px !important;
}

/* 行内代码样式 */
.smart-mp code {
  background-color: rgba(0, 0, 0, 0.05) !important;
  padding: 2px 4px !important;
  border-radius: 3px !important;
  font-family: var(--code-font-family, monospace) !important;
  font-size: 0.9em !important;
  color: #e83e8c !important;
}

/* 分隔符样式 */
.smart-mp-hr-replacement {
  margin: 30px 0 !important;
  height: 2px !important;
  background: linear-gradient(to right, transparent, rgb(236, 68, 68), transparent) !important;
  font-size: 0 !important;
  border: none !important;
}
```
